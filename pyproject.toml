[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "KSP2-Modder-Knowledge"
authors = [{name = "EloxZ", email = "eloxzero@gmail.com"}]
dynamic = ["1.0", "description"]
